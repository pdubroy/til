{
  "private": true,
  "type": "module",
  "scripts": {
    "format": "npx prettier --write '**/*.js'",
    "generate": "npx scripts/generateIndex.js"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
