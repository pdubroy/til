{
  "private": true,
  "type": "module",
  "scripts": {
    "format": "npx prettier --write '**/*.js'",
    "generate": "node scripts/generateIndex.js"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
