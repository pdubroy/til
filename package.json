{
  "private": true,
  "type": "module",
  "scripts": {
    "format": "npx prettier --write '**/*.js'",
    "generate": "node scripts/generateIndex.js"
  },
  "devDependencies": {
    "@humanwhocodes/crosspost": "^1.0.2",
    "prettier": "^3.2.5"
  }
}
